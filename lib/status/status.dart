enum LoadingStatus {
  idle,
  loading,
  error,
  success,
}

enum LoginStatus {
  idle,
  error,
  success,
}

enum RefreshStatus {
  idle,
  refresh,
  loading,
  refresh_no_data,
  loading_no_data,
}

enum ReposStatus {
  idle,
  active,
  inactive,
  loading,
}


